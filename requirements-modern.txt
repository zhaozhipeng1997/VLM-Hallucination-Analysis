# Project — Modern environment requirements (Qwen2.5-VL, InternVL3.5)
# Install: conda create -n env-modern python=3.10 -y && conda activate env-modern && pip install -r requirements-modern.txt
#
# Qwen2.5-VL: Qwen2_5_VLForConditionalGeneration was introduced in transformers 4.49.0
# InternVL3.5: Requires transformers >= 4.52.1 (uses AutoModel + trust_remote_code=True)
#
# Both models work with transformers==4.52.0 as a shared base.

transformers==4.52.0
qwen-vl-utils>=0.0.8
torch>=2.4.0,<2.6.0
accelerate>=1.0.0
pillow>=10.0.0
numpy>=1.26.0
tqdm>=4.65.0
matplotlib>=3.8.0
seaborn>=0.13.0
scikit-learn>=1.5.0
scipy>=1.13.0
pycocotools>=2.0.10
pycocoevalcap>=1.2
pandas>=2.2.0
pyyaml>=6.0
einops>=0.8.0
sentencepiece>=0.2.0
bitsandbytes>=0.45.0

# Flash attention (optional, for speed — recommended for Qwen2.5-VL)
# flash-attn>=2.7.0
